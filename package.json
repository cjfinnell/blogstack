{
  "name": "blogstack",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=24.16.0"
  },
  "scripts": {
    "gen-wrangler": "tsx scripts/gen-wrangler.ts",
    "ci-gen-wrangler": "tsx scripts/ci-gen-wrangler.ts",
    "resolve-deploy-matrix": "tsx scripts/resolve-deploy-matrix.ts",
    "dev:cms": "npm run gen-wrangler && wrangler dev --env dev",
    "dev:web": "tsx scripts/dev-web.ts",
    "db:migrate:local": "npm run gen-wrangler && wrangler d1 migrations apply DB --local --env dev",
    "seed": "tsx apps/cms/scripts/seed-admin.ts",
    "release": "tsx scripts/release.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json && npm run typecheck --workspaces --if-present",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build:fixture": "tsx scripts/build-fixture-site.ts",
    "check:copy": "tsx scripts/check-no-placeholder-copy.ts",
    "seed:site-copy": "tsx scripts/seed-site-copy.ts"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.10.1",
    "astro-eslint-parser": "^3.0.0",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-astro": "^3.0.0",
    "globals": "^17.0.0",
    "prettier": "^3.9.6",
    "prettier-plugin-astro": "^0.14.1",
    "smol-toml": "^1.4.1",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.65.0",
    "vitest": "^4.0.15",
    "wrangler": "^4.52.1"
  }
}
